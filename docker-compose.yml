services:
  aas-hub:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aas-hub
    ports:
      - "3333:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
